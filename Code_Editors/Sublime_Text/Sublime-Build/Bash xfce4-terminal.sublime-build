{
  "cmd": ["xfce4-terminal -e 'bash -c \"chmod +x ${file} && ${file};echo;echo Press ENTER to exit; read line\"'"],"shell": true,
  // "cmd": ["xfce4-terminal -e 'bash -c \"${file};echo;echo Press ENTER to exit; read line\"'"],"shell": true,
  "file_regex": "^(..[^:]*):([0-9]+):?([0-9]+)?:? (.*)$",
  "working_dir": "${file_path}",
  "selector": "source.shell"    
}