{
  "cmd": ["xfce4-terminal -e 'bash -c \"python -u ${file};echo;echo Press ENTER to exit; read line; rm ${file_path}/${file_base_name}.pyc; exit;\"'"],"shell": true,
  "file_regex": "^(..[^:]*):([0-9]+):?([0-9]+)?:? (.*)$",
  "working_dir": "${file_path}",
  "selector": "source.python"    
}