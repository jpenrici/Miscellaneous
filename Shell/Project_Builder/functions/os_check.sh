# -----------------------------------------------------------------------------
# OS check
# -----------------------------------------------------------------------------
_check_os() {
    if [[ "$(uname -s)" != "Linux" ]]; then
        _fail "Linux required. Detected: $(uname -s)"
    else
        _pass "Linux $(uname -r)"
    fi
}